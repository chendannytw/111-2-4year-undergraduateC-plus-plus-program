建立一個結構，成員定義如下:
typedef struct
{
    unsigned int StuNum;
    char Name[20];
    int math,computer;
    double average;
}stuDate;
建立一個程式內含三個副程式，存取隨機檔案內資料，副程式的個別功能如下:
1.建立內容有100筆空白資料的隨機檔案，副程式需有兩個參數:結構指標、資料筆數(只需修改p.488的程式)
2.程式寫入客戶資料至檔案，直到輸入帳戶是0為止(p.489的程式)
3.鍵盤輸入帳號(stuNum)，利用fseek函數，移動檔案指標，將檔案內容全部印出尋找並讀取該筆資料印出，重複此動作直到輸入0為止(p.492的程式)(或是將檔案內容全部印出也可以)

