寫一個主程式及位元位移的副程式，使用三個參數:Value(被旋轉的變數值)、左旋或右旋的設定、bits(旋轉的位元數)，最後印出value位移前與位移後的二進位值。
e.g. shift(int value,int leftORight,int shiftBits)

#include <stdio.h>
void displayBits(unsigned int value, int dir, int num);
int main()
{
    unsigned int x;
    printf("%S", "Enter a nonnegative int:");
    scanf_s("%u", &x);
    displayBits(x, 1, 1);
}
void displayBits(unsigned int value, int dir, int num) {
    unsigned int c, displayMask = 1 << 8;
    printf("%10u=", value);

    //loop through bits
    for (c = 1; c < 9; ++c) {
        putchar(value & displayMask ? '1' : '0');
        value << 1;
        if (c % 8==0)
            putchar(' ');
    }
}
